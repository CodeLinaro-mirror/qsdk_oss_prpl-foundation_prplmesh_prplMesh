# AUTO GENERATED FILE
# GENERATED BY generate_ci_yml.py WITH A LIST OF TESTS TO INCLUDE

R4:mozart:MAP-4.2.1:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.1:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.2.2:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.2:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.2.3_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.3_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.2.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.2.4_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.4_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.2.4_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.2.4_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.3.2_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.3.2_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.3.2_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.3.2_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.3.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.3.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.3.4_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.3.4_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.3.4_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.3.4_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.4.2_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.4.2_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.4.2_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.4.2_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.4.3_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.4.3_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.4.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.4.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.3_BHWIFI_FH5GH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.3_BHWIFI_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.3_BHWIFI_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.3_BHWIFI_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.3_ETH_FH5GH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.3_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.5.3_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.5.3_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.6.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.6.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.6.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.6.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.4_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.4_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.4_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.4_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.5_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.5_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.5_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.5_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.6_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.6_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.6_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.6_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.7_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.7_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.7_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.7_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.8_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.8_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.8_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.8_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.9_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.9_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.9_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.9_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.10_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.10_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.7.10_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.7.10_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.2_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.2_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.2_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.2_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.3_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.3_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.5_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.5_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.5_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.5_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.6_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.6_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.8.6_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.8.6_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.9.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.9.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.2_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.2_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.3_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.3_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.4_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.4_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.5_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.5_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.10.6_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.10.6_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.1_BH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.1_BH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.1_BH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.1_BH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.2_BH5GL_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.2_BH5GL_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.2_BH24G_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.2_BH24G_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.2_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.2_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.3_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.3_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.4_BH5GL_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.4_BH5GL_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.4_BH24G_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.4_BH24G_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.6:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.6:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_BH5GL_FH5GL:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_BH5GL_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_BH5GL_FH24G:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_BH5GL_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_BH24G_FH24G:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_BH24G_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_ETH_FH5GH:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_ETH_FH5GL:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.13.11_ETH_FH24G:
  extends: .certification:R4:mozart_downstream_R1_agent
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.13.11_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_BH5GL_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_BH5GL_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_BH5GL_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_BH5GL_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_BH24G_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_BH24G_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_ETH_FH5GH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.1_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.1_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_BH5GL_FH5GL:
  extends: .certification:R4:mozart_on_boot_5
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_BH5GL_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_BH5GL_FH24G:
  extends: .certification:R4:mozart_on_boot_24
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_BH5GL_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_BH24G_FH24G:
  extends: .certification:R4:mozart_on_boot_24
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_BH24G_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_ETH_FH5GH:
  extends: .certification:R4:mozart_on_boot_5
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_ETH_FH5GL:
  extends: .certification:R4:mozart_on_boot_5
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.14.2_ETH_FH24G:
  extends: .certification:R4:mozart_on_boot_24
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.14.2_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.16.1_ETH_FH5GH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.16.1_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.16.1_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.16.1_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.18.1_BHWIFI_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_BHWIFI_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.18.1_BHWIFI_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_BHWIFI_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.18.1_ETH_FH5GH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_ETH_FH5GH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.18.1_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.18.1_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.18.1_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.19.1_BHWIFI:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.19.1_BHWIFI:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-4.19.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-4.19.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.4.1_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.4.1_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.4.2_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.4.2_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.4.3_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.4.3_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.5.1:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.5.1:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.6.1:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.6.1:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.7.1:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.7.1:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.8.1_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.8.1_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.8.2_ETH:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.8.2_ETH:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.8.3_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.8.3_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.13.1_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.13.1_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.13.2_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.13.2_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.14.1_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.14.1_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.14.2_ETH_FH24G:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.14.2_ETH_FH24G:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true

R4:mozart:MAP-5.16.1_ETH_FH5GL:
  extends: .certification:R4:mozart
  rules:
    # If the last commit description contains the test name and we're not on master, run it.
    # Otherwise, make it a manual job
    - if: '$CI_COMMIT_DESCRIPTION =~ /.*MAP-5.16.1_ETH_FH5GL:mozart.*/ && $CI_COMMIT_REF_NAME != "master"'
      when: on_success
    - when: manual
      allow_failure: true
